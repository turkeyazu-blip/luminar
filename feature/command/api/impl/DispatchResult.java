package ru.luminar.feature.command.api.impl;

public enum DispatchResult {
   NOT_DISPATCHED,
   DISPATCHED;

   // $FF: synthetic method
   private static DispatchResult[] $values() {
      return new DispatchResult[]{NOT_DISPATCHED, DISPATCHED};
   }
}
